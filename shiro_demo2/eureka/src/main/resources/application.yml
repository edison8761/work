server:
  port: 8761                                        #服务端口号
spring:
  application:
    name: eureka-server                            #服务名
eureka:
  server:
    enable-self-preservation: false               #防止自我注册
    eviction-interval-timer-in-ms: 3000
  client:
    fetch-registry: false                          #获取注册
    service-url:
      defaultZone: http://localhost:8761/eureka   #注册中心的地址

